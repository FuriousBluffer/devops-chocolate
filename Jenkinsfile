pipeline { 
agent any 
    stages { 
        stage ('Build') { 
            steps { 
              sh "python sniffle.py"
                    echo 'Running build phase. '
              
                }
        }
    }
}
